pub mod monomial {
	pub fn greatest_common_divisor(input1: i64, input2: i64) {
		// Sorts inputs so that a is greater than b.
		let (a, b) = match input1 > input2 {true => (input1, input2), false => (input2, input1)};

		// TODO
	}
}
